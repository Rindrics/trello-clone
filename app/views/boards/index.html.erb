<h1>Boards</h1>

<div>
  <% @boards.each do |board| %>
    <% unless board.archived? %>
      <div class="board">
        <%= link_to board.name, board %>
      </div>
    <% end %>
  <% end %>
</div>

<div>
  <%= link_to "New Board", new_board_path %>
</div>
